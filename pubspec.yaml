name: lint_mut_testy
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.0

# Add regular dependencies here.
dependencies:
  path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  custom_lint:
  lint_mut_infect:
    # git:
    #   url: https://github.com/0xNF/dart_lint_mut_infect
    path: ../lint_mut_infect
